<h1>Books</h1>

<table>
<thead>
<tr>
<th>title</th>
<th>body</th>
<th></th>
</tr>
</thead>

<tbody>
<% @lists.each do |list| %>
<tr>
<td>
<%= list.title %>
</td>
<td>
<%= list.body %>
</td>
<td>
<%= link_to "Show", todolist_path(list.id) %>
<%= link_to "Edit", edit_todolist_path(list.id) %>
<%= link_to "Destroy", destroy_todolist_path(list.id), method: :delete, "data-confirm" => "Are you sure?" %>
</td>
</tr>
<% end %>
</tbody>
</table>


<h3>New book</h3>

<%= form_for(@list, url: '/todolists') do |f| %>

<% if @list.errors.any? %>
<%= @list.errors.count %>errors prohibited this book from being saved:
<br>
<% @list.errors.full_messages.each do |message| %>
<%= message %><br>
<% end %>
<% end %>

	<h4>Title</h4>
	<%= f.text_field :title %>
	<h4>Body</h4>
	<%= f.text_area :body %>
	<br>
	<%= f.submit 'Create Book' %>
	<% end %>
